# Meowtion Cloud Functions: server-side model training + authenticated clip upload.
firebase-functions>=0.4.0
firebase-admin>=6.4
numpy>=1.24
tensorflow-cpu>=2.15     # cpu-only build, smaller image, fine for these tiny models
scikit-learn>=1.3
