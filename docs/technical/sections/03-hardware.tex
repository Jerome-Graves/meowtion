\section{Hardware}
\label{sec:hardware}

The build is two boards: the collar worn by the cat and the station plugged in at home.
Both are based on Seeed Studio XIAO modules, chosen for their small footprint, integrated
radios, and hand-solderable castellated pads.

\subsection{Collar}

The collar is built around the Seeed XIAO nRF52840 Sense. The nRF52840 provides a Bluetooth
Low Energy radio and enough flash and RAM to run quantised neural-network inference. Critically
for Meowtion, the Sense variant carries the two sensors the system needs on the module itself:
a six-axis inertial measurement unit (IMU) and a MEMS microphone, so the collar needs no
external sensor breakout.

\begin{table}[ht]
  \centering
  \caption{Collar components.}
  \label{tab:collar-bom}
  \begin{tabularx}{\textwidth}{@{}lX@{}}
    \toprule
    \textbf{Part} & \textbf{Role} \\
    \midrule
    Seeed XIAO nRF52840 Sense & SoC (BLE), IMU and PDM microphone on-module; runs inference \\
    LiPo cell                 & Battery power for the wearable \\
    Enclosure + strap         & 3D-printed housing and collar strap (see \texttt{hardware/}) \\
    Acoustic membrane         & Protects the microphone port while passing sound \\
    \bottomrule
  \end{tabularx}
\end{table}

The IMU supplies the motion stream that is always classified; the microphone supplies the
short audio window used to disambiguate behaviours that look alike by motion but differ by
sound (\cref{sec:on-device-ai}). The microphone port sits behind an acoustic membrane in the
housing; correct membrane placement matters for audio quality and is documented in the hardware
guide.

\begin{figure}[ht]
  \centering
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{exploded.png}
    \caption{Exploded view of the collar assembly.}
    \label{fig:exploded}
  \end{subfigure}\hfill
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{internals.jpg}
    \caption{Assembled internals.}
    \label{fig:internals}
  \end{subfigure}
  \caption{Collar hardware. Full parts, print files, and assembly steps are in
           \texttt{hardware/README.md}.}
  \label{fig:collar-hw}
\end{figure}

\subsection{Station}

The station is a Seeed XIAO ESP32-S3. The ESP32-S3 has both Wi-Fi and BLE, so a single module
acts as the BLE central that listens to the collar and the Wi-Fi client that reaches the cloud.
It is mains-powered over USB, so it can scan and stay online continuously without the power
constraints of the wearable. The station carries no sensors of its own; its job is to hear the
collar, capture training clips when asked, and bridge to the internet.

\subsection{Why split the system this way}

The collar must be small, light, and run for a useful time on a small cell, which rules out
Wi-Fi on the wearable (its power draw is incompatible with a collar-sized battery). BLE is
low-power but short-range and not an internet transport. The fixed, mains-powered station
resolves both: it gives the collar a cheap always-on BLE peer and a Wi-Fi path to the cloud.
This division also keeps the privacy boundary clean: the heavy, sensitive processing stays on
the collar, and only a classification result crosses to the station.
